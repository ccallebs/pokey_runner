require 'spec_helper'

describe PokeyRunner do
  it 'has a version number' do
    expect(PokeyRunner::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
